<%= form_for(@template_model) do |f| %>
  <% if @template_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@template_model.errors.count, "error") %> prohibited this template_model from being saved:</h2>

      <ul>
      <% @template_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
